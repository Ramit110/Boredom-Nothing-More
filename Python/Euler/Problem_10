Mathses = __import__("Euler_Lib")

def Chal12(limit):# Finds first triangular num in range 
    tri,x = 0,1
    while True:
        tri+=x
        x+=1
        if(len(Mathses.Factorify(tri)) >= limit):
            return tri

print(Chal12(500))
